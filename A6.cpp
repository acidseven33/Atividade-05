#include <iostream>
using namespace std;

int sub ( int *A){
    int S;
    *A = 2 / (*A);
    S= *A
    return (S);
}

int main() {
    int A;
        cin A;
        S = S_sub (&A)
        cout << "A" << " " << "B" << endl;
        cout << "sub" <<sub(S) << endl;
        return 0;
}
